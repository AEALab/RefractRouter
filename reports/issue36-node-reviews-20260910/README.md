# #36 独立节点评审停止记录

2026-09-10，按用户“确认”执行现有 21 份节点材料的 GLM-5.3 独立评审，
额度为 210 AFP，底层零重试、首错停止。原校准、K3 基线、Pro 参考与 21 份探针均复用。

结果：**blocked / incomplete**。仅第一份评分完成并通过校验；第二次请求在 120 秒后
超时，未返回用量，余下 19 次请求未派发。未自动重跑、选模或执行组合 DAG。

## 实际调用与费用

| 项目 | 本轮实测 |
|---|---:|
| 计划评审份数 | 21 |
| 已派发请求 | 2 |
| 成功响应及有效评分 | 1 |
| 超时请求 | 1 |
| 尚未派发请求 | 19 |
| 已知评审费用 | 2.2194 AFP |
| 本阶段完整费用 | 未知 |
| 新增生产调用 | 0 |
| 已执行请求区间墙钟时间 | 204.903814 秒 |

第一份请求耗时 84.829 秒，`finish_reason=stop`，`attempts=1`；
第二份耗时 120.066 秒，`failure_type=timeout`，`attempts=1`。
两个 `request-finish` 事件分别记录成功和失败，不表示有两份有效评分。

已知费用由返回用量复算：
`(1676 输入 token + 3256 输出 token) / 1000 × 0.45 = 2.2194 AFP`。
其中 2791 个推理 token 已包含在输出内，缓存输入为零；不重复计费。
超时请求没有供应商请求 ID 或用量返回，费用保留 `null`。
不能将 `210 - 2.2194` 当成已确认可用余额，也不将未知请求的原预留 6.90075 AFP
当成实际账单或严格费用上界。本轮没有新增外层 DSH 模型调用。

## 唯一有效评分与解释边界

盲样本 `f99a2dc67bd9ebf8a42fe75c` 对应 `parse_requirements / strong`（Pro）：
正确性 34、来源支撑 26、完成度 16、下游可用性 8，总分 **84**。
GLM 认为主题、章节及约束提取完整，但受众属于未明确标注的推断，且没有独立受众字段。
逐字引用与中文理由在[原始响应](output/responses.ndjson)及
[部分评分](output/partial-reviews.json)中保留。

84 分低于该候选的冻结质量下限 85，但其余 20 份评分尚缺，不能据此判定整个节点或
整条路线无可行解。缺失分数没有填零，也未用确定性契约上限替代独立质量分。
本轮未生成可直接导入组合阶段的 `reviews.json`；没有完整路由质量或收益结论。

## 冻结与审计

执行代码为 `e32ad7c8b009a69836f141690c4731536d79eac6`，与已确认的预检完全匹配。
端点为 `https://ark.cn-beijing.volces.com/api/plan/v3/chat/completions`，
模型 `glm-5.3`，输出上限 8192，thinking enabled，120 秒超时，串行、零重试。
公开材料与原校准、私有状态、质量阈值和生产快照均未修改。

- [阶段授权](authorization.json)：21 份、210 AFP、范围及排除项。
- [冻结预检](output/preflight.json)：准确请求、材料/代码哈希与费用预留。
- [请求进度](output/model-progress.ndjson)：两次请求的起止及结果。
- [阶段汇总](output/summary.json)：已知费用、未知完整费用及停止原因。
- [审计](audit.json)：核对 5 个新索引产物、12 个原材料索引产物、费用及唯一有效评分。
- [审计脚本](audit_reviews.py)：零调用复算；已有审计存在时只比较，不覆盖。
- [输出索引](output/evidence-index.json)与[本次完整归档索引](archive-index.json)。

复算命令：

```bash
uv run python reports/issue36-node-reviews-20260910/audit_reviews.py
```

下一步应先核对超时请求用量，并为其余材料冻结明确的检查点续跑方案与请求等待策略。
任何重新派发都属于新的请求，不能掩作 SDK 自动重试；已有有效评分应保持原样。
本次授权批次已因停止条件终止，不自动扩展为再次尝试、组合、最终评审、pilot 或 final。

#36 继续开放，#22 的完整重复对照要求以及 #5 → #6 → #7 → #8 的依赖均未满足。
旧零调用报告保留为执行前快照，新增结果仅写入本目录和当前交接文档。

按 craft-wiki 评估，本轮“未知用量停止依赖调用”的候选已被现有前置条件失败规则覆盖，
跳过重复摄入；一个节点评分及一次超时不支持新的通用质量或性能结论。
