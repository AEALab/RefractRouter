# Kimi-K3 单次重试结果

2026 年 9 月 8 日，按照用户“再试一次？”的指令，仅重试 Kimi-K3 整篇基线。
本次成功返回，状态为 `baseline-ready`；报告契约通过，尚未进行独立质量评审或 DAG 对比。

## 执行结果

| 项目 | 结果 |
| --- | --- |
| 生产模型与请求数 | `kimi-k3`，1 次 |
| 请求耗时 | 227.560 秒 |
| 等待上限 | 300 秒，前次为 120 秒 |
| 输入 / 输出 token | 1194 / 7960 |
| 命中缓存 token | 1024，属于输入子集 |
| 结束原因 | `stop`，没有输出截断标记 |
| 自动重试 | 0 次 |
| 参考路线 / 节点探针 / 独立评审 | 均为 0 次 |
| 生产费用 | 9.154 AFP |
| DSH 外层 | Flash 2 次，0.2394 AFP |
| 本次合计 | 9.3934 AFP |

模型、任务、提示、输出上限 8192 和关闭思考的请求配置保持不变。
本次耗时超过原先的 120 秒阈值，说明这次请求需要更长等待；由于没有受控重复实验，
不能据此认定上次超时的唯一原因，也不能推导通用的 300 秒配置。

[原始基线报告](baseline.html)直接提取自最终状态，未人工改写。
检查确认必须章节存在、内部链接可解析，未发现外部脚本或样式标签。
记录中的 `task_score: 90` 是确定性评分，并非 GLM 或人工评审分数；
它不证明所有论断的证据准确性，也不代表实验质量门槛已通过。

## 费用与授权边界

本次生产保护额度收窄至 20 AFP，继承此前 132 AFP 的生产授权，
用户本次只授权一次 K3 重试。运行结束后已关闭隔离 DSH 配置的付费开关。
保护额度不是服务端硬账单上限。

前次准备阶段已知费用为 4.98315 AFP，本次为 9.3934 AFP，
两次已知合计为 **14.37655 AFP**。前次 K3 超时及被中断的 Pro 请求费用仍未知，
完整累计费用保留为空，不能用已知合计替代。此前 GLM 校准的 8.68545 AFP 另计。
前次与本次 DSH 外层累计 0.3512 AFP，低于原授权 0.5 AFP。
费用依据冻结 Agent Plan 清单与接口返回用量核算；缓存输入不重复计入。

## 证据与验证

- [核算记录](audit.json)：请求数、耗时、用量、费用及未知项。
- [原生工具证据](dsh-evidence.json)与 [DSH 最小事件记录](outer-events.json)。
- [原始输出目录](output/benchmark-summary.json)：按原始字节归档，索引未改写。
- [校准复用依据](calibration-reuse.json)：在付费前核对任务、模型和生成器快照，
  将已有校准复制至新预检目录；未重新调用 GLM。
- [验证记录](verification.json)：完整测试 200 项通过、TypeScript 类型检查通过；
  原生输出哈希和前次归档的 19 个文件哈希均核对通过。

## 后续工作

下一步应增加从已验证的 `baseline-ready` 材料继续参考路线与节点探针的入口，
核对任务、模型、代码和校准绑定，并将历史费用与新增费用分别记录。
现有 `prepare` 仍从头生成 K3；不得直接使用它来声称复用本次基线。
完成恢复入口及零调用验证后，再按剩余阶段的授权边界推进 DAG 与独立盲评。
本次只拿到了 K3 基线，还没有“DAG 更便宜且质量相当”的结论。
