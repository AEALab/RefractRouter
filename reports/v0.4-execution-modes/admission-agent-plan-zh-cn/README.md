# 单轮真实 A/B/C 对照：简体中文报告

**阅读提示：自动评审给全 Flash 100 分，但内容复核发现报告没有完成实质平台比较。
不能据此认定它质量最佳。下一步应先校准评审，再补强一次调用契约；详见
[内容复核与下一步](content-review.md)。**

本轮已按用户授权执行。候选 Flash、M3、Pro，独立 K3 评审；同一冻结来源包与任务，
全部组别使用简体中文章节与正文要求。温度 0、关闭思考、输出上限 8192、零重试。
所有请求经过原生 DSH 工具，仅使用 Agent Plan `/api/plan/v3`。

## 结果与完整性

基准完整性：**不完整**；费用和证据审计：**通过**。
单轮只用于准入和诊断，不能证明统计稳定收益；失败结果的分数仅用于诊断。

| 策略 | 可纳入比较 | 最终评审分 | 路线费用（AFP） | 关键路径（毫秒） | 完成节点 |
|---|---|---:|---:|---:|---:|
| 单模型七节点／deepseek-v4-flash | 是 | 100.0 | 0.689 | 67339 | 7 |
| 单模型七节点／minimax-m3 | 是 | 95.0 | 5.01575 | 67581 | 7 |
| 单模型七节点／deepseek-v4-pro | 是 | 87.0 | 10.05345 | 112377 | 7 |
| 逐节点选模 | 是 | 91.0 | 4.33895 | 68058 | 7 |
| 一次调用／deepseek-v4-flash | 否 | 69.0 | 0.36275 | 69076 | 0 |
| 一次调用／minimax-m3 | 是 | 94.0 | 1.416 | 32300 | 1 |
| 一次调用／deepseek-v4-pro | 否 | 58.0 | 3.4045 | 78718 | 0 |

路线费用不含探针和评审。失败原因及原始分数见 [核对结果](reviewed-results.json)。

## 同任务同轮次配对

| 对照 | 有效对数 | 质量差 | 费用差（AFP） | 延迟差（毫秒） |
|---|---:|---:|---:|---:|
| 单模型七节点／deepseek-v4-flash 对比 一次调用／deepseek-v4-flash | 0 | 不可用 | 不可用 | 不可用 |
| 单模型七节点／minimax-m3 对比 一次调用／minimax-m3 | 1 | 1.0 | 3.59975 | 35281 |
| 单模型七节点／deepseek-v4-pro 对比 一次调用／deepseek-v4-pro | 0 | 不可用 | 不可用 | 不可用 |
| 逐节点选模 对比 单模型七节点／deepseek-v4-flash | 1 | -9.0 | 3.64995 | 719 |
| 逐节点选模 对比 单模型七节点／minimax-m3 | 1 | -4.0 | -0.6768 | 477 |
| 逐节点选模 对比 单模型七节点／deepseek-v4-pro | 1 | 4.0 | -5.7145 | -44319 |
| 逐节点选模 对比 一次调用／deepseek-v4-flash | 0 | 不可用 | 不可用 | 不可用 |
| 逐节点选模 对比 一次调用／minimax-m3 | 1 | -3.0 | 2.92295 | 35758 |
| 逐节点选模 对比 一次调用／deepseek-v4-pro | 0 | 不可用 | 不可用 | 不可用 |
| 逐节点选模 对比 七节点组内事后最佳 | 1 | -9.0 | 3.64995 | 719 |
| 逐节点选模 对比 一次调用组内事后最佳 | 0 | 不可用 | 不可用 | 不可用 |

差值为前者减后者。所有指标使用相同有效配对集合；任何候选失败时，组内事后最佳基线不可用。
不能从失败组中只挑选成功候选来宣称已找到最优模型。独立节点最优也不是全局组合最优。

## 新证据交接验证

- 单模型七节点／deepseek-v4-flash：synthesize_analysis：未转抄证据数组，引用 3 处；write_report：未转抄证据数组，引用 21 处。
- 单模型七节点／minimax-m3：synthesize_analysis：未转抄证据数组，引用 9 处；write_report：未转抄证据数组，引用 36 处。
- 单模型七节点／deepseek-v4-pro：synthesize_analysis：未转抄证据数组，引用 3 处；write_report：未转抄证据数组，引用 18 处。
- 逐节点选模：synthesize_analysis：未转抄证据数组，引用 10 处；write_report：未转抄证据数组，引用 32 处。

上述检查验证状态交接与引用身份，不替代来源语义核对，也不能把历史失败重判成成功。

## 费用与审计

- 已返回请求：79／启动 79；预检最多 80 次。
- 生产：39.10205 AFP；节点评审：78.716 AFP；最终评审：42.805 AFP。
- 外层 DSH：0.2217 AFP；本轮总计：**160.84475 AFP**。
- 原始索引核对 24 个文件；21 个节点候选单元逐条核对输出与上下文哈希。
- 唯一计费记录与服务端请求一一匹配，事后复用不重复计费；付费配置已关闭。
- 总授权 3000 AFP 下，扣除累计已知与历史保留额后余额为 1495.84985 AFP。

[授权](authorization.json)、[审计](audit.json)、[账本](cost-accounting.json)、[节点矩阵](node-quality-matrix.md)、
[详细配对](strategy-comparisons.md)、[DSH 记录](dsh-evidence.json)。

## 执行边界

没有重试、自动重跑、追加重复、扩展候选或媒体调用，也没有公开推送。
原始模型和评审响应不作修补；面向阅读的报告与说明使用简体中文，机器字段及来源标识保留。
下一步应依据本轮完整性与配对结果提出范围明确的建议，不能只凭单轮分数扩大付费实验。
