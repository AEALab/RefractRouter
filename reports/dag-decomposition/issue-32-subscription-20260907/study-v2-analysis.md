# 真实 DAG 对照结果

运行状态：`failed`。已取得评分的测试运行 0/54；独立测试任务 3 个。
未完成或模拟数据不能用于宣称真实路由收益。

## 测试完整性与描述性指标

| 方法 | 已评分/计划 | 验收通过 | 平均质量 | 平均部署 AFP | 墙钟 p50 毫秒 | 墙钟 p95 毫秒 |
|---|---:|---:|---:|---:|---:|---:|
| direct-strong | 0/9 | 0 | 不可用 | 不可用 | 不可用 | 不可用 |
| dag-strong-serial | 0/9 | 0 | 不可用 | 不可用 | 不可用 | 不可用 |
| dag-strong-parallel | 0/9 | 0 | 不可用 | 不可用 | 不可用 | 不可用 |
| dag-calibrated-single | 0/9 | 0 | 不可用 | 不可用 | 不可用 | 不可用 |
| dag-node-a | 0/9 | 0 | 不可用 | 不可用 | 不可用 | 不可用 |
| dag-node-b | 0/9 | 0 | 不可用 | 不可用 | 不可用 | 不可用 |

这些均值仅描述各组已评分样本；若样本不完整，不得跨行直接比较。部署成本包括节点和最终评审；
小样本 p95 使用线性插值，只是本批样本的描述，不构成时延 SLA。

## 同样本配对比较

配对证据不足，未输出路由收益判断。

区间按任务聚类重采样；同一任务的重复不是独立任务。这里只有 3 个任务，结果不支持泛化证明。

## 全量用量账本

| 类别 | 已确认 AFP 估算 | 未确认预留 AFP |
|---|---:|---:|
| 生产 | 5.01975000 | 0.00000000 |
| 评审 | 15.04300000 | 0.00000000 |

AFP 按服务返回用量和冻结费率估算，用于比较与记账；订阅制下不应直接解释为新增现金账单。
失败调用保留在全量账本中，缺失质量不填零；本表不含其他修复轮次或 DSH 外层用量。

## 证据与边界

协议 SHA-256：`b00899f12e7942e80dd07c42a742f893b5dc78259f1162c4acbe4a5c28669b3f`。原始产物哈希已逐项核对。
原始问题状态：`["node-output-contract-invalid: expected JSON object"]`。
固定人工 DAG 不验证自动规划器的拆分质量；实际任务入口需另看 DSH 会话和核心产物。
