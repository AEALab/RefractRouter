# Issue #32 订阅验证与剩余验收

本轮基于 `13827056c0dbf24026f0cdf7fa027c8557aa1ad1` 的本地未提交实现。
已完成真实分层校准及三个格式问题的修复回放，但全量留出对照失败，不能宣称节点路由收益。
当前仅第 1、2、3、5 项通过；第 4、6、7 项仍未完整验收。

## 已完成与实际限制

- `study-v3` 完成 36 次整图校准、84 条独立节点观测；生成 9 行实测 profile，覆盖三个能力分层及声明输入预算区间 `[8193,32769)`，没有排除项。不能外推到任意自动 DAG。
- 留出测试共保存 17 次运行：16 次取得最终评分，1 次节点契约失败；计划为 54 次。已评分样本只覆盖 1/3 个独立任务。
- 第 348 次调用中，`compare_test_a--3--dag-node-a:risk` 返回额外 `covers` 字段。两个已派发分支均结算，汇总节点未启动，最终评审未调用，零重试。
- A/B 已评分结果均为同一模型分配，尚未证明实际跨模型交接。A/B 的选择可全部相同，不能强制混用模型制造收益。
- 校准单模型基线按质量优先，尚无对称的整任务 A/B 最优单模型基线；结果不能用于宣称普遍优于三指标整任务路由。
- DSH 插件的安装、契约与生命周期已验证。真实外层助手尚未执行：自动审批认为 DSH 自动传递仓库指令给 Ark 需要额外上下文传输授权，已向用户说明并询问。不是 AFP 预算阻塞。

## 修复与证据完整性

三次真实格式失败分别为 Markdown JSON 围栏、未转义英文双引号和多余规划元数据。
逐次增强输出指令并单独冻结回放版本；严格解析器不去围栏、不修复响应、不删除多余字段。
每轮三个候选各一次结构回放通过，不调用下游或评审，也不推导语义质量或可靠性收益。
最新提示词明确输出键集合必须与 `contract.output.fields` 完全一致。
原 v3 留出样本已用于修复诊断，后续独立验收须冻结新留出集；不得拼接部分轮次冒充完整通过。

v2/v3 协议绑定完整 Python 核心源码指纹；实现变化会在预检前拒绝。v3 原实现另存
`implementation-v3.tar.gz`，与当时运行环境摘要一并保留。当前代码已改变，因此历史协议不能
直接在当前源码上重跑。所有原运行目录及其内层索引保持原样，后续分析在目录外单独保存。

## 本轮用量

| 运行 | 状态 | 真实调用 | 生产 AFP 估算 | 评审 AFP 估算 |
|---|---|---:|---:|---:|
| replay-01 | contract-valid | 3 | 0.65865000 | 0.00000000 |
| study-v2 | failed | 36 | 5.01975000 | 15.04300000 |
| replay-02 | contract-valid | 3 | 0.73765000 | 0.00000000 |
| study-v3 | failed | 348 | 66.10915000 | 168.82800000 |
| replay-03 | contract-valid | 3 | 0.71595000 | 0.00000000 |

本轮合计 393 次真实调用，全部有用量记录，未知费用预留为零。
历史首轮另有 1 次调用、0.0275 AFP，保存在相邻 `issue-32-paid-20260907/`，未并入上表。
AFP 用于用量记账与成本比较；订阅制下不直接解释为新增现金账单。用户本轮无需逐项预算确认的偏好已记入 `AGENTS.md`。

## 验证

- 完整回归：230 项测试、5 项子测试通过，包含执行 26 项 TypeScript 插件契约的测试；两者不重复相加。
- 历史三个失败响应分别保持严格拒绝；新增回归核对多余字段、停止汇总与已发调用结算。
- 报告入口核对原始哈希；明确 1/3 任务覆盖、17 次尝试中的 1 次失败，不把缺失质量填零。完整模拟的报告生成也通过，模拟结果不输出真实收益结论。
- 所有当前与历史原始索引逐项复核；源码、配置、文档和本轮外层产物另存 `verification-index.json`。

## 证据定位

- [最终全量尝试分析](study-v3-analysis-final.md)：真实测试完整性、描述性指标、全量账本和基线限制。
- [第三次失败诊断](diagnosis-v3.json)：精确字段差异、下游停止和留出集暴露状态。
- [本轮用量汇总](accounting-summary.json)；[完整测试日志](pytest.log)。
- `study-v2/`、`study-v3/`：冻结协议、逐调用原始请求/响应、账本、独立观测、计划、节点时序与索引。
- `replay-01/`、`replay-02/`、`replay-03/`：三轮候选模型严格结构验证及原始响应。
- [DSH 上下文授权状态](dsh-approval-status.json)：自动审批拒绝记录；进程未启动，真实宿主调用为零。

## 未完成清单

1. 冻结新的未观察留出集、修订实现及对称的单模型 A/B 基线，再取得完整多任务配对结果；不得重用已暴露样本作为独立验收。
2. 完成真实异构交接与最终独立评估，扩大能力/风险/输入规模覆盖；实测没有混合分配时如实报告。
3. 用户允许 DSH 向 Ark 传递仓库指令后，从隔离宿主执行一次自然任务入口，核对会话中的真实工具调用和核心产物；当前准备使用迁移 profile，不当作新分层 profile 的任意任务验证。
4. 实际自动规划还需与固定人工 DAG 分别报告拆分质量、规划费用和端到端耗时；固定图对照不替代自动规划验收。
