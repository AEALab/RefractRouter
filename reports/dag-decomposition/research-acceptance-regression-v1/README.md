# 验收条目传递修复的真实回归

首批真实先导发现人工计划、直接回答和节点探测的生成请求缺少完整验收条目。
研究执行器现将原始条目显式追加到生成任务正文，不修改任务要求或独立评分标准。
本回归重复已观察的 SQLite 材料，只验证修复，不作为新的留出或路由收益证据。

## 结果

| 路线 | 首批结果 | 修复回归 | 回归 AFP | 回归墙钟时间 |
| --- | --- | --- | ---: | ---: |
| 直接回答 | 88，未通过 | 100，通过全部条目 | 2.13235 | 22.86 秒 |
| 人工 DAG | 90，未通过 | 100，通过全部条目 | 3.80975 | 34.67 秒 |

回归共 6 次真实调用全部结算，生产 **2.9051 AFP**、评审 **3.037 AFP**，合计 **5.9421 AFP**。
与首批合计为 48 次真实调用、**52.3174 AFP**，未超过初始 84 次调用与 2219.2129 AFP 包络。
AFP 按服务端确认的 token 用量及冻结清单费率复算，不把预留额当作实际费用。
所有 HTTP 尝试次数为 1，未对原批次的评审格式错误自动重试。

7 个归档文件哈希通过，生成请求逐条核对包含全部原始验收条目，费用复算与总账一致。
三项输入回归测试修复前失败、修复后通过；完整测试结果为 583 passed、5 subtests passed，耗时 63.14 秒。

## 证据与范围

- [修复回归协议](protocol.json)、[原始响应与账本](output/session.json)。
- [审计结果](audit.json)、[审计脚本](audit.py)、[文件完整性索引](output/artifact-index.json)。
- [首批原始失败及诊断](../research-live-pilot-v1-actual/README.md)保留原样，不覆盖或重评分。

协议中的 `implementation_sha256` 是本次实际执行的代码快照；内嵌 `config` 保留首批配置作为来源。
该修复只作用于独立研究执行器，未修改既有 K3 runner 或历史实验原始证据。
原始旧协议仍绑定原代码快照，不允许在修复代码下悄悄重跑旧协议。

这次实测确认修复后的两条路线能收到并满足遗漏条目，但不能从单次重复推断统计因果收益。
正式独立材料、完整 A/B、真实异构交接及 #40 人工抽检仍需单独完成。
