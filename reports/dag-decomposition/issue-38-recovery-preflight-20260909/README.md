# Issue #38 恢复对照预检与模拟验收

真实模型调用为 **0**。本目录只证明实验入口和确定性故障路径可用，不证明真实可靠性、
质量、成本或时延收益。[Issue #38](https://github.com/AEALab/RefractRouter/issues/38)
仍待真实运行授权、采集、解释与跨 Issue 回填，不能关闭。

## 协议与包络

[预检](preflight/preflight.json)及[冻结协议](preflight/protocol.json)绑定当前核心实现、
模型清单和开发集 profile。协议 SHA-256：
`d84b9573b8e57ddba34cc44f1175c96e49d0d0ad1afeee9b98efb1ed98a3038c`。

三个新编写的材料任务，每个重复两次，三组共 18 个计划样本。
初始模型为 MiniMax M3，替换模型为 DeepSeek V4 Pro，最多一次切换；独立评审为 Kimi K3。
没有使用 cheap 全图路线，因为既有开发集已因契约失败排除其所需的分析分层。

| 类别 | 最大调用数 | 完整保守上限 |
| --- | ---: | ---: |
| 生产，包含失败及重跑 | 90 | 1000.2432 AFP |
| 独立最终评审 | 18 | 737.28 AFP |
| 合计 | 108 | 1737.5232 AFP |

这些是按输入输出上限计算的授权包络，不是预估实际支出，更不表示已经获准调用。
具体停止条件、样本解释和执行参数见[实验说明](../../../docs/recovery-study.md)。

## 确定性故障证据

[模拟结果](offline/result.json)使用合成 profile、确定性客户端和网络禁用保护，
在 cheap 的 risk 节点注入未闭合 JSON。此模拟场景仅用于检查机制，与真实协议的
生产模型次序和留出任务分别保存。

| 组 | 结果 | 上游 cost 节点执行次数 |
| --- | --- | ---: |
| 不恢复 | 失败，保留已消费账本 | 1 |
| 局部节点换模型 | 完成，仅替换 risk | 1 |
| 全图换模型重跑 | 完成，下一尝试全图使用 mid | 2 |

共 13 次模拟调用；逐次用量均已结算。局部 risk 的两次输入 SHA-256 相同，
整图重跑仍包含废弃分支费用。原始请求、响应、分配、时序、评审和费用见结果文件及
[逐文件哈希索引](offline/artifact-index.json)。模拟分数与费用不能作为真实收益。

另外执行真实协议的 `--demo` 全流程：18 个样本完成、72 次模拟调用、零真实调用。
没有注入错误的模拟结果只检查流程，不能用于估计真实恢复触发率。

## 回归与历史边界

完整 `uv run pytest`：**535 项通过**，用时 51.94 秒，无网络模型调用。
18 个模拟产物的 SHA-256 索引核对通过；`git diff --check` 通过。

确定性测试覆盖三组行为、相同上游、成功兄弟复用、在途结算、跨尝试提供方间隔、
模型顺序与次数、质量/容量/预算/截止准入、取消、未知用量及证据失败停止，
以及已结算评审失败后的独立样本继续和缺失评分不填零。

K3 原始基线增加当前代码的精确兼容记录：核对非代码配置完全相同，
新增实验路径不被 K3 的 DeepAgentsGraphExecutor 使用。
仅复用原始成功基线，仍禁止已完成探针跨快照 compose/finalize；历史报告未被改写。

## 下一步

按上述协议和双预算取得本次真实调用授权，使用全新目录运行。
保留全部计划样本交付率、费用、失败、触发次数及共同已评分配对集合。
如果未触发任何真实恢复，须明确说明不能估计恢复收益，不能通过模拟补成真实结果。
