# v2 三轮验证准备

2026-09-07，PR #33 已合并到 `1f7cbd22157f90c1cf2c68099b321adf620fd316`，#29 已关闭。
#22 仍待完整三轮对照。本目录是零调用预检及待批准方案，没有新增模型输出或实验结论。

## 已完成验证

从干净合并提交运行 `validation/dsh/real_runner.py`，本地 Python 边界预检为 pass，
并非已通过 DSH 外层模型执行。输入及代码哈希见 [边界证据](boundary-evidence.json)。
141 项离线测试通过，TypeScript typecheck/build 通过。

模型、judge、价格快照、端点、提示词契约、节点 rubric、执行策略和估算假设均与
单轮准入相同；仅将 repeats 设为 3。候选为 Flash/M3/Pro，独立 judge 为 K3，
显式使用 `exclude-known-contract-rejections-v2`，temperature 0、thinking disabled、
8192 输出上限、零重试。新增模型目录不改变本次冻结模型池。

[预检](preflight.json)计划生产 168 次、节点评审 63 次、最终评审 15 次，共 246 次模型调用。
实际调用数可能因契约拒绝或其他失败而减少；不能据此把缺少的对照算作完成。

## 待批准预算

| 项目 | AFP |
|---|---:|
| 已有总授权 | 2505 |
| 已知用量及旧未结算请求保留额 | 967.19845 |
| 当前剩余 | 1537.80155 |
| 三轮生产估算 | 716.89 |
| 三轮评审估算 | 1262.98 |
| 三轮合计估算，不含外层 | 1979.87 |
| 建议生产额度 | 730 |
| 建议评审额度 | 1270 |
| 建议外层 DSH 额度 | 0.5 |
| 本轮合计额度 | 2000.5 |
| 旧用量与保留额加本轮额度 | 2967.69845 |

建议将总授权由 **2505 增至 3000 AFP（增加 495 AFP）**。
其中本次只启动一组全新三轮，额度 2000.5 AFP；未分配余量为 32.30155 AFP。
外层历史用量 4.4315 AFP，加本轮 0.5 AFP 仍在原 5 AFP 外层总额度内。
低于估算的历史实耗不作为绕过当前预检或增加调用范围的理由。

本方案尚未获得新增额度批准，付费开关保持关闭。
[运行方案](run-plan.json)同时保存无调用请求与待批准的付费请求，后者当前不得执行。

## 执行与验收

1. 批准预算后，复核干净提交及输入哈希，从原生 DSH 插件启动，使用新的输出目录。
2. 仅使用 Ark Agent Plan `/api/plan/v3`，重试为零；结束或失败后关闭 paid profile。
3. 三轮必须保存 63 格完整矩阵、有效输出和上下文哈希、选择记录；合格候选有独立评审，
   已知契约拒绝保留原因、零契约分和费用，缺失信息继续阻断。
4. 三轮 composed node-oracle 均执行并独立评审，9 个单模型报告和 15 个策略观测完整。
   质量、成本、延迟必须使用一致配对集合，另外核算包含失败请求的全部用量。
5. 完成原始证据、请求遥测、费用与离线复核审计，再更新 #22；完整性与路由变化守卫
   通过后才解释原有收益门槛。没有预设 Go 结论。

不把单轮准入或旧 v1 轮次拼成三轮，不在本授权中运行 pilot、final、新模型准入或媒体调用，
也不自动重跑失败实验。下一阶段必须根据完整证据与剩余额度另行确定。
