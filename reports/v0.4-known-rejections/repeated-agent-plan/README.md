# v2 全新三轮对照

2026-09-07，从干净提交 `34e1bd6`，通过独立 DSH profile 的原生验证工具执行。
用户明确同意将总授权由 2505 增至 3000 AFP；本轮生产 730、评审 1270、外层 0.5 AFP。
本次仅运行 report_001 的全新三轮，未与单轮准入或历史 v1 结果合并。

DSH 状态：**fail**；benchmark 完整性：**incomplete**；
冻结收益门槛判定：**Insufficient-evidence**。

三轮混合路线均完整执行并独立评审，质量为 **94、90、92**。阻断完整比较的是第三轮
Flash 单模型路线：分析输出遗漏必需的 `evidence` 字段，后续节点停止，最终评审缺失。
#22 继续保持开放，#5 pilot 暂不启动。

## 协议与完整性

候选 Flash/M3/Pro，独立 K3 judge；显式 v2 排除已知契约拒绝。prompt/checks v0.3、
节点 rubric v0.2、temperature 0、thinking disabled、8192 输出上限、零重试均保持冻结。
所有请求仅使用 Ark Agent Plan `/api/plan/v3`。新增模型目录没有扩展本轮模型池。

- 请求完成 242/242，成功 242；预检计划 246 次。
- 保存 63 格矩阵、9 个单模型产物、15 个策略产物。
- 矩阵评估状态：`{'judged': 63}`。
- 原始索引校验 39 个文件，输出/上下文哈希及唯一请求费用核对通过。
- finish reason：`{'stop': 242}`；attempts：`{'1': 242}`。

预检 246 次与实际 242 次的差额有明确来源：Flash 分析节点失败后，三个下游生产请求
和一次最终 judge 未执行。全部实际 API 请求正常返回，不等于全部完整路线成功。
9 个单模型产物中 8 个有独立最终评审；15 个策略产物中 13 个有有效最终评审。

原始 `oracle_gate.matrix_complete=false` 是冻结实现对整个运行完成性的守卫名称，
不能解释为矩阵缺格。`node_matrix` 实际为 63/63，三轮 `node_availability` 的记录与评审
均完整；本次不改写历史字段或原始判定。

## 各策略有效轮次与配对比较

| 策略 | 平均质量 | 平均路线 AFP | 关键路径 p95 ms | 有效轮次 / 预期 |
|---|---:|---:|---:|---:|
| node-oracle | 92.0 | 5.50523333 | 96785 | 3/3 |
| node-type-rule | 85.667 | 3.90506667 | 77635 | 3/3 |
| strong-all | 88.333 | 7.70696667 | 105662 | 3/3 |
| task-oracle | 94.5 | 2.7692 | 61174 | 2/3 |
| weak-all | 90.5 | 0.568325 | 49694 | 2/3 |

每行质量、路线成本、延迟采用该策略同一成功且独立评审集合；路线成本不含探针及评审。
各策略有效轮次不同，不能直接跨行相减。以下差值仅使用同轮有效配对，
完整逐轮对照见 [配对比较](strategy-comparisons.md)。

| 对照 | 有效对数 | 质量差均值 | 成本差均值 AFP | 延迟差均值 ms |
|---|---:|---:|---:|---:|
| node-oracle vs strong-all | 3 | 3.66666667 | -2.20173333 | 6425 |
| node-oracle vs task-oracle | 2 | -2.5 | 1.985325 | 35185.5 |

与全 Pro 的三轮对照有较高质量及较低路线成本，但与同轮最佳单模型的前两轮对照为
质量低 2.5 分、成本高 1.985325 AFP、关键路径慢 35.1855 秒。后者没有显示路由收益。
第三轮最佳单模型对照不完整，不能据此作三轮 Go 或 No-go 判定。

## 失败定位及后续工作

[零调用诊断](failure-diagnosis.json)确认：第三轮 Flash 的证据抽取结果包含 8 条证据，
随后的分析响应只有 `analysis`，缺少冻结 v0.3 契约要求的 `evidence`。
`invalid-evidence` 及三个 `upstream-failure` 与原始输出一致，未发现检查器误判的证据。

v2 处理的是独立候选探针的已知拒绝，无法修复完整单模型路线的契约失败。本轮全部 63 格
探针均合格，因此不能声称本次实时触发验证了 v2 的拒绝排除分支。

下一步先离线评估证据保留的责任边界及完整路线失败的比较规则，再冻结后续协议。
可比较保留模型透传证据契约、由 Python DAG 状态携带不可变证据两种方案；
后者仍需验证分析中的引用准确性，不能靠补字段把失败响应改算成功。
本次未改变协议、补造失败报告、对失败基线赋语义分数或自动重跑。

## 用量与审计

- 生产（含探针）：104.12595 AFP。
- 评审：271.772 AFP。
- 外层 DSH：0.209 AFP。
- 本轮合计：**376.10695 AFP**。
- 累计已知用量及旧未结算保留额：1343.3054 AFP。
- 总授权 3000 AFP 下尚余：**1656.6946 AFP**。

费用由探针、唯一单模型执行、真实组合/规则执行及独立 judge 记录分别复算。
基线策略复用单模型结果，不重复计费。保留旧未结算请求 16.192 AFP；未导出账户级账单。
付费 profile 已关闭，未自动重跑，也未启动 pilot、final、新模型或媒体调用。

[授权](authorization.json)、[账本](cost-accounting.json)、[审计](audit.json)、
[DSH evidence](dsh-evidence.json)、[外层 usage](outer-agent-usage.json)、
[原始索引](evidence-index.json)、[矩阵](node-quality-matrix.json)、[失败分类](failure-taxonomy.md)。

[独立离线复核](../repeated-review/reviewed-summary.json)保留了相同的有效样本集合和
Insufficient-evidence 判定，额外模型调用为零。

## 解释边界

这是同一 canonical task 的三次重复，用于检查完整性和任务内波动；不能替代多任务 pilot、
最终基准或人工审计。节点局部最优组合也不代表所有模型组合的全局最优。
下一阶段依据本轮完整证据决定，不预设路由收益或自动放行 pilot。
