# Task 质量小样本第二批中断记录

用户将累计授权上限提高到 1000 AFP。新批次 `task-quality-pilot-v3` 使用
80,000 字节请求上限、每任务最多 6 次调用，按运行时预留公式计算新批次上界
839.5008 AFP；加首批已结算 29.983 AFP 后为 869.4838 AFP。

第二批四个校准样本完整结束：LRU 缺陷两条固定路线都通过隐藏测试；存储资料
固定 `deepseek-v4-flash` 通过确定性检查，固定 `deepseek-v4.1-flash` 未通过。
第一个保留样本（slug、固定 v4 Flash）到 6 次调用上限后未完成，保留失败记录。
第六个样本（slug、固定 v4.1 Flash）第三次调用收到
`Response incomplete: length`，没有可确认模型用量；执行器立即停止整个批次，
没有派发任何 Task 直选调用，也没有运行资料研究保留题。

结构化记录见 `task-quality-pilot-paid-v3-records.jsonl`。前五个样本确认结算
43.41305 AFP；第六个样本前两次调用另确认结算 5.30075 AFP。连同首批
29.983 AFP，**确认结算总计 78.6968 AFP**。第六个样本第三次调用保留
**16.55475 AFP** 预留，用量待核对；若按全额计入，当前实验额度占用为
**95.25155 AFP**。预留不是已证实的供应商扣费，不把它写为实付金额。

未确认调用的运行 ID 为
`6a18523c2308d4763e5940cd76f4928d275064d5dfd312249941f889285e92b5`，
本机原始运行记录在
`/tmp/refractrouter-task-quality-pilot-20260926-c/workspaces/05-code-tdd-slug-static-v4.1-flash/.refractagent/runs/planning/`。
原始记录包含完整请求和工具内容，暂不提交仓库。此次调用没有可用于提供方核对的
`request_id`；在核对前不重发该操作，也不把它混入完整任务质量统计。

下一步只可作为**新的独立批次**运行未尝试过的保留题，引用已完整校准的四条
记录作为短能力卡来源。新批次须重新冻结任务、调用范围和费用上界，并把上述
16.55475 AFP 按最坏情况计入累计授权限额。
