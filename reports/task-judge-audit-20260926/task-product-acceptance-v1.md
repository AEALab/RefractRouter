# Task 逐候选路由产品接线验收

## 冻结范围

2026-09-26 使用现有 DSH `headless` profile，在独立临时工作区执行一次
`pwd` 原生工具任务。模型池为 Ark `deepseek-v4-flash`（low）和
`deepseek-v4.1-flash`（high）；轻量 LLM Judge 使用前者。禁用委派和 HTTP
自动重试，最多三次模型调用，单次输出上限 2048 tokens，任务期限五分钟。

零调用预检摘要：`9e7a05d8d7d86f30bf38747e76b051692cd4bf502805439dc26bdd077ee6f199`。
新批次上界为 61.728 AFP；按此前已确认费用与未知用量预留计算的累计上界为
195.59775 AFP，低于用户授权的累计 2000 AFP。原始预检、请求日志及运行账本
保留在 `/tmp/refractrouter-task-product-acceptance-20260926-a/`；不覆盖历史批次。

## 观察结果

DSH 退出码为 0，任务状态为 `completed`，历时 11.433 秒。Judge 对两个候选
均返回适合度信号 0.9、信息不足信号 0.1。Python 在通过初始门槛后，按首次
执行调用的保守费用上界选择 `deepseek-v4-flash`：2.96835 AFP，对照
`deepseek-v4.1-flash` 的 14.84175 AFP。这些是预留上界，不是本任务实际花费。

| 用途 | 实际模型 | 结算 AFP | 工具调用 |
|---|---|---:|---:|
| Judge | deepseek-v4-flash | 0.0392 | 0 |
| 首轮执行 | deepseek-v4-flash | 0.51505 | 1 |
| 工具续接 | deepseek-v4-flash | 0.51855 | 0 |

本次已确认生产费用合计 **1.0728 AFP**。宿主执行一次 `pwd` 工具调用后，
模型根据实际工具结果回答路径；未创建 DAG，也没有再次调用 Judge。
三条模型调用记录均为 `billed`，无未知用量或保留预留。

用户此前所有已确认批次累计为 117.315 AFP；加入本次后为
**118.3878 AFP**。另有历史未知用量预留 16.55475 AFP，按上界计的累计
占用为 **134.94255 AFP**。未知预留不是已确认扣费，也不能自动释放重用。

## 结论边界

这次验收证明轻量 LLM Judge、Python 逐候选排序、原生工具续接和 AFP 结算
在该路线能协同工作。一个短任务不能校准 0.8 门槛，也不能证明两个候选均能
可靠完成更复杂的任务。当前排序只使用首次执行调用上界；完整任务预计费用和
可核对的时延排序尚未实现。本地 Laya `choice-v2` 仍缺少所有候选的独立适合度
证据，因此不会被描述为已验收的成本感知路由。
