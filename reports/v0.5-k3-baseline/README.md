# K3 主对照的离线验收

本归档验证新主对照的执行与评审流程：Kimi-K3 一次完成任务，对比质量达标后
按节点选择最低费用模型并执行的 DAG。**本次新增真实模型调用为零、付费支出为零。**
没有产生 K3 与 DAG 的真实质量或收益结论，当前也没有新一轮付费授权。

## 已完成验证

- `uv run pytest`：184 项通过，包含 TypeScript 插件契约、真实 Python 子进程零调用预检、
  评审身份跨阶段一致性、校准拒绝、材料哈希检查、费用选模和分阶段模拟执行。
- 插件 0.7.0 类型检查与编译通过；打包内容、隔离导入契约通过。
- DSH `0.1.1-rc.2`、Node `22.22.3`、pnpm `10.15.0` 的打包生命周期验证通过：
  安装、覆盖配置、卸载、重装和启动。未发送模型请求。
- [历史完整性核对](historical-integrity.json)：三份历史索引的 47、58、39 个文件哈希全部一致。
- [Python 宿主预检](dsh-preflight-evidence.json)：通过，实际调用为零；此归档来自本地
  DSH Python 包装入口，原生注册工具的零调用路径另由完整测试验证。

## 本次产物

| 产物 | 用途 | 不能据此声称 |
|---|---|---|
| [冻结预检](preflight/README.md) | 配置、代码、两份随机编号校准材料与空白模板 | 评审已校准 |
| [离线模拟](offline/README.md) | 模拟 36 次生产调用、节点选模、盲评导入与成本汇总 | 真实质量、实际省钱或 Go |
| [实验设计](../../docs/k3-baseline-comparison.md) | 主对照、评分规则、费用口径和阶段交接 | 阈值已被证明最优 |

离线最终状态保留 `Simulation-only` 和校准未通过；模拟分数不得用来放行付费。
离线费用字段是夹具值，实际新增费用为零。`preflight/private` 与 `offline/private`
包含策略映射，不应交给盲评者；真实校准应使用 `preflight/public` 中的材料。

## 下一步

先确定独立评审身份并完成校准，再确认分阶段预算，最后执行真实对照。
独立人工和独立模型都使用同一评分导入协议；没有选定一个自动调用的默认评审模型。

准备阶段计划 29 次生产调用，预估 131.67 AFP，按当前未舍入预检需额度至少 131.68 AFP；
组合阶段最多 7 次，预估 46.94 AFP。全部生产预估 178.61 AFP，
**不含最多 25 个外部评审样本项和 DSH 外层费用**。输入 token 数为估计，非严格费用上界。
具体交接及限制见实验设计。历史余额不能作为本次新授权。

比较同时保留最终路线成本和首次使用成本；参考路线与节点探针计入后者。
单轮即使出现描述性优势，也保持证据不足，不自动扩大到多任务实验。

本次按 Craft Wiki 评估一个候选结论：路由是否带来实际节省。由于没有新真实对照证据，
未满足验证门槛，跳过该结论的经验沉淀。
