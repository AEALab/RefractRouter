完成。

在 `slug.py` 实现了 `slugify`，并新增 `tests/test_slug.py`（15 个测试）。

**实现要点（仅标准库）：**
- NFKD Unicode 规范化，丢弃组合标记（如 `café → cafe`）
- 拉丁字母转小写，保留数字
- 中文字符（CJK 扩展 A + 统一表意文字）原样保留
- 空白、标点及其他分隔符合并为一个连字符
- 去掉首尾连字符；空结果返回空字符串

**验证：** `python3 -m unittest discover -s tests` 全部通过（15/15），覆盖空字符串、标点/空白合并、中英文混合、变音符号规范化、全角字符转换、首尾连字符去除及 None 等场景。

未读取或修改 `.refractagent`。
