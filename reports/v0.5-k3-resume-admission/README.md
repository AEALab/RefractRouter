# DAG 恢复阶段执行受阻

用户于 2026 年 9 月 8 日确认本阶段新增 120.5 AFP 授权：生产 120 AFP，
DSH 外层 0.5 AFP。授权已记录，但本次未进入原生恢复工具，参考路线与节点探针均未开始。
成功 K3 基线和恢复预检保持原始字节不变。

## 执行情况

1. 输入索引、评审文件、代码快照、K3 基线硬契约、GLM 校准与插件 0.9.0 核对通过。
2. 两次申请扩展权限启动均被自动审批以超时原因拒绝；没有创建 DSH 进程。
   超时不是安全风险判定。
3. 使用普通沙箱启动，DSH 在创建模型会话前因文件监听 `EMFILE` 退出。
   原始记录完整保留在 `startup-failure/`。
4. 本机 Chokidar 源码明确支持 `CHOKIDAR_USEPOLLING`。仅在隔离 DSH 环境中启用
   轮询后，零调用 `--help` 启动成功；未修改应用或依赖源码。
5. 正式任务的外层 Flash 会话返回 `TRANSPORT: Connection error.`，没有调用
   `refractrouter_validate`，因此没有新增参考路线、探针、K3 或评审请求。
6. 包装脚本已关闭隔离 DSH 配置中的付费开关，没有自动重复失败会话。

## 费用与授权

生产请求数为 0，生产费用为 0 AFP。外层有一次失败模型步骤，记录的 token 用量为零，
但没有服务端请求编号或计费凭证；外层费用及本次完整费用保留未知，不将连接失败
当作服务端免费结算的证明。此前两笔未知生产费用仍独立保留。

本次失败没有产生可用于节点评审的材料，也没有验证 DAG 的质量或成本收益。
用户的授权仍保留；后续无需重新确认相同阶段预算。需要先恢复允许 DSH 连接 Agent Plan
的执行权限或环境，再根据本次无生产调用的证据处理下一次启动。不得覆盖本目录、
删除失败记录后盲目重跑，也不得绕过 DSH 原生调用边界。

## 证据

- [已确认授权](authorization.json)与[启动前核验](prelaunch-verification.json)。
- [执行汇总](execution-summary.json)与[最小外层事件](outer-events.json)。
- [运行环境调整](startup-recovery.json)、[错误日志](dsh-stderr.txt)及[宿主退出状态](host-exit.json)。
- [完整性核验](verification.json)：K3 重试归档 33 个文件、恢复预检归档 10 个文件均未变化。

本轮未修改生产代码；之前通过的 212 项离线测试结论保持原范围，不代表此次真实连接成功。
