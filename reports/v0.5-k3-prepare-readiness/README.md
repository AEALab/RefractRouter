# K3 主对照准备阶段准入

2026-09-08，准备阶段零调用预检通过，已完成的 GLM 校准评分与当前任务、代码快照匹配。
本次仅完成执行准备，新增生产请求、评审请求和外层模型请求均为零。

## 拟执行范围

| 内容 | 模型 | 最多调用 |
|---|---|---:|
| 整任务基线 | Kimi-K3 | 1 |
| 固定上游参考路线 | DeepSeek-V4-Pro | 7 |
| 七节点三模型探针 | Flash、M3、Pro | 21 |
| 准备阶段生产合计 | 上述四个模型 | 29 |

单任务、单轮、零重试。完成后停止在 `node-review-ready`，等待独立节点评审。
本阶段不调用 GLM，不执行所选 DAG，也不做最终报告对比评分。
GLM 两例校准已经通过，可复用其评分导入，不重复花费两次校准费用。

## 建议授权额度

- 生产额度：**132 AFP**。冻结生产预估 131.6736 AFP，最低两位小数额度为 131.68 AFP。
- DSH 外层额度：**0.5 AFP**，使用 Flash 分发一次原生验证工具调用并汇报结果。
- 内部及外部评审额度：**0 AFP**。
- 本次拟申请合计：**132.5 AFP**。

生产预估使用冻结清单、每调用输入 4000 token 与输出上限 8192 token。
输入数量是估计，程序在下一次请求前检查预算；不是服务端硬账单上限。
外层额度单独监控和记账，不混入 DAG 路线费用。
此次拟申请范围不包含后续 21 项独立节点评审、七节点组合及最终两份盲评。

上次授权仅覆盖 GLM 的两次校准，已执行完成。未使用校准额度不自动转给生产。
[拟执行参数](proposed-run.json)中的 `authorized: false` 表示当前尚未获得本阶段授权。

## 已完成核对

- Python 代码逐文件哈希与 `v0.5-glm-calibration/main-preflight` 的冻结配置一致。
- 真实 `reviews.json` 与冻结校准材料匹配，重新导入得到 28／91 分且校准通过。
- [DSH Python 包装入口预检](dsh-preflight-evidence.json)通过，阶段计划为 29 次生产、零评审。
- 隔离 DSH profile 启动验证通过，插件版本为 0.7.0，付费开关仍为关闭。
- 外层 Flash 使用 Agent Plan 专用端点，输出上限 512 token、零重试；通用 shell、
  文件修改、子代理等工具保持禁用，只保留本实验原生工具所需环境。
- 前一次完整回归为 195 项通过；此后 Python 和 TypeScript 实现均未变化，未重复运行测试。

隔离配置位于本机临时目录 `/private/tmp/refractrouter-k3-prepare-dsh`，不将环境配置或凭据
提交到仓库。插件部署的评审配置上限因现有配置类型限制保持为 1，但实际请求明确传
`maxEvaluationCost: 0`，且 Python 准备阶段没有评审调用。

## 执行后留证

获批后仅发起一次 `refractrouter_validate` 准备调用，并使用其全新输出目录。
结束后关闭该隔离 profile 的付费开关，核对请求数、重试、用量、基线契约、参考路线
和 21 格探针记录，保存原始响应及节点盲评材料。
遇到预算或执行异常保留已有证据，不自动重跑整阶段；不会用本阶段成功状态代替
完整 K3／DAG 对照结论。
