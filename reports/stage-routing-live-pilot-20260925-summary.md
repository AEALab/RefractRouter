# Stage 真实小样本执行记录（2026-09-25）

本记录保留一次授权范围内的全部执行尝试。各目录均未覆盖或自动重跑。

| 批次 | 模型调用 | AFP | 结果 |
|---|---:|---:|---|
| 初版 | 0 | 0 | 补丁路径按工作区重复解析 |
| v2 | 0 | 0 | `web` profile 不接受 headless task 参数 |
| v3 | 0 | 0 | 运行证据目录位于工作区外，被沙箱拒绝 |
| v4 | 0 | 0 | 用户全局 Static 设置覆盖实验 Stage 配置，触发数据域拒绝 |
| v5 | 0 | 0 | 实验输入包络被误写成模型上下文容量，保守准入失败 |
| v6 | 5 | 15.65375 | 原生工具循环完成；宿主规范退出码未传入 Python，未发生预期升级 |
| v7 | 5 | 23.2942 | Flash → Pro → Flash 正确；展示描述进入指纹，理由误记为 `tool-signal` |
| v8 | 5 | 23.1789 | `stage-v3` 全部检查通过，完成 Flash → Pro → Flash 真实功能验收 |

v4 后为实验工作区配置独立设置文件，未改动用户的全局规划路由设置。v5 后将模型容量改为
Ark 官方 1,024,000 token，并继续保持实验 65,536 输入、8,192 输出限制。v6 暴露了 DSH
消息投影和工具规范结果之间的差异；修正只传递宿主规范事实，Stage 分类仍由 Python 完成。

v7 证明结构化宿主事实可以触发升级、保持并恢复高效模型。验收仍诚实记为失败，因为第三轮
理由应为 `repeated-failure`，实际记录为 `tool-signal`。根因是模型为两次相同 Bash 命令生成了
不同的展示 `description`；该字段不影响执行，却参与了旧失败指纹。`stage-v3` 已从 shell 指纹中
排除该字段，并增加确定性回归；修复后的真实复验由 v8 完成。

v8 使用新的 `stage-v3` 冻结指纹执行，模型序列、决策理由、调用上限、AFP 上限、最终标记与
DSH 完成状态全部通过。该结果证明当前受控轨迹的真实切换机制可用；不代表 Stage 已在代码和
资料研究任务上优于两条 Static 基线，收益仍须通过尚未执行的 72 次冻结实验验证。
