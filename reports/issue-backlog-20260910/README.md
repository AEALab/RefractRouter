# 七项历史验证 Issue 的处理记录

日期：2026-09-10。关联 #1、#5、#6、#7、#8、#22、#36。
当前结果为 **incomplete**。本轮新增真实模型调用 **0**，新增模型费用 **0 AFP**。
代码入口、离线回归和预检完成不等于真实实验完成；这七项 Issue 保持开放。

## 本轮完成

- 建立专用开发分支及 #36 的 `89ad632` 冻结 worktree。
- 新增 GLM-5.3 独立节点/最终评审入口，复用通过的校准，保留原始回答、部分评分、
  实测费用、未知项与首错停止记录；默认零调用。
- 修复人工 finalizer：固定 10 分门槛、严格数值、四个样本与哈希绑定、审核身份/时间、
  五维分数、逐条主张核对、原文依据、严重事实错误以及证据不足的判定。
- 从 #22 最新 v2 归档重新核对 39 个原始索引文件、63 格节点输出/上游及 242 个请求。
  原始归档保持原始字节；新复核位于独立目录。
- 完成 #36 的节点独立评审及冻结代码下 `compose` 预检，以及 #22、pilot、final
  三轮调用数与费用预检。

完整用法、依赖和执行边界见[交接说明](../../docs/benchmark-validation-handoff.md)。

## #22 的最新实证状态

[零调用审计](issue22-evidence-review/reviewed-audit.json)确认 242 个请求均正常返回、
一次尝试；63 格节点评审完整，三条 node-oracle 路线均完成。
第三轮 Flash 单模型因缺少 `evidence` 停止，最佳单模型评审不完整。

| 同轮对照 | 有效对数 | node-oracle 质量差 | node-oracle 生产费用差 AFP |
|---|---:|---:|---:|
| 对 strong-all | 3 | +3.66666667 | -2.20173333 |
| 对 task-oracle | 2 | -2.5 | +1.985325 |

数值来自[配对结果](issue22-evidence-review/reviewed-strategy-comparisons.json)。
对 task-oracle 的两对描述性结果没有收益信号；第三对缺失使完整三轮判定仍为
`Insufficient-evidence`。不补零、不跨轮拼接，也不据此放行 #5。

## 下一阶段范围与费用

| 阶段 | 生产调用 | 评审调用 | 原冻结 token 假设预留 AFP | 历史均费情景 AFP |
|---|---:|---:|---:|---:|
| #36 独立节点评审 | 0 | 21 | 209.72025 | 不适用 |
| #36 组合 DAG | 7 | 0 | 46.94 | 待选模 |
| #22 新三轮 | 168 | 78 | 1979.87 | 402.37112 |
| #5 三轮 pilot | 1155 | 525 | 13856.14 | 2797.07448 |
| #6 三轮 final | 2310 | 1050 | 27712.27 | 5594.14896 |

各项预检分别见 [#36 节点](issue36-node-review-preflight/summary.json)、
[#36 组合](issue36-compose-preflight/preflight.json)、[#22](issue22-preflight/preflight.json)、
[pilot](pilot-preflight/preflight.json)、[final](final-preflight/preflight.json)。

#36 节点评审建议上限 **210 AFP**，只覆盖现有 21 份材料，不重新生成校准或生产结果。
组合、两份最终评审以及其他研究阶段不自动承接这个额度。两份最终材料尚未产生，
因此最终评审预算待材料形成后再冻结。DSH 外层费用单列，表中未纳入。

历史用量情景从已核验的 242 个唯一请求估算，生产 104.12595 AFP、评审 271.772 AFP
与原账本一致。各模型输入 p95 为 2561—5029 token，输出 p95 为 801—2293 token，
明显低于准入假设的输出 8192。依据[逐模型统计与公式](budget-analysis.json)，
固定候选调用乘各自历史均费，其余生产调用乘候选最高均费，评审调用乘 K3 judge 均费。
该情景来自一个任务的三次重复，不能作为新任务费用上界；不据此减少输出上限或
修改冻结准入预留。实际 pilot 完成后才能重新核定 final。

## 当前未满足的条件

#36 缺新一阶段明确授权与真实评分；#22 缺完整三轮比较；#5/#6 缺前置验收与各自授权；
#7 缺 final 产物和人工审核；#8/#1 缺最终完整证据。这里的 incomplete 是当前状态，
不将正常的依赖等待写成实验已执行失败，也不把这些 Issue 关闭为完成。

pilot 的五个测试任务与 final 测试集重叠，不能称为全部未观察的最终样本。
正式审核只覆盖冻结的四个产物，不证明其余样本均正确。

## 验证与知识评估

- `uv run pytest`：566 passed，包含 55 项 TypeScript 宿主契约测试。
- 插件 `typecheck` 与 `build`：通过；生成目录未纳入提交。
- 三份关键历史归档的 12、10、39 个索引产物哈希均一致。
- 独立评审预检：21 项、实际调用 0；冻结代码的组合预检：7 项生产、实际调用 0。
- `git diff --check` 与本报告内部链接核对：通过。

首轮完整测试发现旧插件测试仍要求跨新代码恢复旧基线；修正为明确拒绝未经登记的迁移后，
完整套件通过。本轮不新增兼容白名单，冻结代码中原组合入口的预检已实际通过。

craft-wiki 只评估“失效前置证据阻断依赖付费阶段”这一候选；既有
“付费流水线的前置条件失败边界”已覆盖本次决策规则和适用边界，因此跳过重复摄入。

三个 DSH 本地包装器预检均为 `pass`、`mode=preflight`、`issues=[]`：
[dry-run](dsh-dry-run-preflight.json)、[pilot](dsh-pilot-preflight.json)、
[final](dsh-final-preflight.json)。它们记录 `invoked_by=local`，不表示新建了真实 DSH
模型会话。[机器可读验证记录](verification.json)绑定实现提交 `a65209e`。
