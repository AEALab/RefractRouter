# K3 准备阶段超时与停止记录

2026-09-08，本次已获授权的准备阶段未完成。Kimi-K3 整任务请求在 120 秒超时，
没有返回可评分报告；因此无法形成有效的 K3 主对照，判定为证据不足。
这不能说明 K3 的报告质量差，也不能说明 DAG 优于 K3。

## 实际执行

DSH 外层 Flash 只发起了一次原生 `refractrouter_validate`，参数与获批方案完全一致。
冻结程序未在基线失败时停止，随后进入全 Pro 参考路线。发现问题后停止宿主及其子进程，
避免继续执行无法用于本轮主对照的生产调用；隔离 profile 的付费开关已经关闭。

| 请求 | 状态 | 费用 AFP |
|---|---|---:|
| K3 整任务基线 | 120.086 秒后客户端超时；未返回用量 | 未知 |
| Pro 参考路线前五个节点 | 正常返回 | 4.87135 |
| Pro 参考路线第六个请求 | 停止时尚未返回 | 未知 |
| 节点探针、GLM 评审、组合 DAG | 未执行 | 0 |
| DSH 外层 Flash | 一次请求，分发原生工具调用 | 0.1118 |

共发起 **7 次生产请求**，其中 5 次正常返回、1 次客户端超时、1 次停止时未完成。
所有已完成请求均无重试。参考路线尚未完成，21 格探针矩阵没有开始生成。
宿主响应 SIGTERM 后以退出码 0 结束，这不代表实验成功；缺少完整的阶段汇总和交接材料。

## 费用口径

**已知费用为 4.98315 AFP；本次总费用未知。**
生产额度 132 AFP、外层额度 0.5 AFP 不变，但未收到用量的两次请求不能按零费用处理，
也不能用额度减已知费用就宣称余额可用于下一轮。

失败适配器在原始 `production-results.ndjson` 中为超时请求保留 `cost: 0` 占位，
原始记录保持不改；[中断核对汇总](interruption-summary.json)明确将该请求和未完成请求
标为 `cost: null`，将 `total_cost` 标为未知。
后续需按调用时间、模型与请求记录核对服务端账单。本次没有追加请求查询或重新生成产物。

## 已完成的离线修复

修复 `prepare` 在基线失败后仍继续付费执行的问题：

- K3 请求失败、最终契约不合格或产物为空时，立即返回 `blocked / baseline-failed`。
- 不进入参考路线和节点探针，并保存基线失败检查点。
- 有调用尝试但没有输入／输出用量时，阶段总费用为未知，已知费用另列。

新增三个回归覆盖超时、无效 HTML 及 CLI 检查点和未知费用，完整 `uv run pytest`
为 **198 项通过**。全部使用离线夹具，没有重试本次 K3 请求。
此修复不改变 120 秒请求时限，也不能解决尚未确定原因的 K3 超时。

## 证据与后续

- [授权](authorization.json)、[单次工具调用任务](dsh-task.txt)、[外层事件与用量](outer-events.json)。
- [生产请求进度](raw-output/model-progress.ndjson)、[已完成原始结果](raw-output/production-results.ndjson)。
- [冻结预检](raw-output/preflight.json)、[主动停止记录](operator-stop.json)、[中断核对](interruption-summary.json)。
- [历史完整性](historical-integrity.json)、[离线修复验证](verification.json)、[文件索引](archive-index.json)。

下一步先核对未知费用，并单独分析 K3 请求时限或服务端状态；不要直接重跑完整 29 次。
实现已经修改，后续真实运行需要新代码预检和明确的新范围授权，旧冻结目录不能直接重跑。
若任务和校准材料保持完全相同，可在新预检中显式迁移其绑定并复用评分，不能修改旧归档
或冒用新随机编号来匹配已有评审。本次未实施新的付费计划。
