# PR #35 冲突与兼容验证

本次将主分支 `6543e91` 合入 PR #35 的 `codex/issue32-fair-baselines` 分支。
主分支包含 PR #37 的执行对照与 K3 分阶段实验；PR #35 保留 DAG 对照及有界节点恢复。

## 修复内容

DSH 契约测试在同一位置修改了测试辅助函数调用与回退参数。
合并后使用 `localProcessContext({}, 'refractrouter_task')`，同时传递
`maxNodeFallbacks: 1` 并检查 Python 结果中的恢复策略，保留非法参数拒绝测试。

首次完整测试另发现两项 K3 恢复失败：新增 Python 文件和共享客户端改动改变了
冻结配置指纹，原有精确兼容记录因此不再匹配。这是版本校验正常拒绝，并非恢复运行失败。

核对后向 `data/benchmarks/k3-baseline-resume-compatibility.json` 追加兼容记录，
绑定原始成功基线索引、源配置及目标配置
`a2dcaef0b37faeb417bfd067ce32678f2e94d17cff802ea02743c96e6f58fc2d`，
并列出相对原始基线的全部代码差异哈希。

相对本次合入的主分支，K3 runner、适配器、任务、提示、模型、契约、校准和价格未变。
共享客户端增加原始用量与可用性字段，并严格识别整数计数。新增测试根据归档结果构造
离线 HTTP 响应，确认成功基线的输出、输入输出及缓存与推理计数、结束原因、请求 ID、
尝试次数和 `9.154 AFP` 估算保持一致。这不等同于重放当时未归档的原始 HTTP 响应，
也不将结论扩大到缺失或异常用量。

## 验证与边界

- 针对 K3 和 DSH 的 40 项测试通过，包括实际 Python 子进程的零调用恢复预检。
- 精确源配置、目标配置与索引约束测试通过；未登记的代码变化仍被拒绝。
- 历史 `node-review-ready` 材料的跨版本 `compose` 仍被拒绝。
- 基线恢复测试确认原始证据文件哈希不变；历史报告保持原样。
- `uv run --offline pytest`：374 项通过（51.62 秒），包含插件构建与契约测试。
- 插件严格类型检查通过；相对最新主分支的新增改动通过空白检查。

本次没有发起真实模型调用，也没有新增真实节点恢复实验。兼容记录仅允许复用指定成功
基线，不能据此迁移已完成的节点探针、组合或最终评审结果。
