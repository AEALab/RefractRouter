# PR 集成与冻结版本交接

本次将 `codex/evidence-state-execution-modes` 的实验实现和历史证据提交审查，关联
Issue #36。主分支集成基准为 `831f71f`；保留其文本任务接口及 Python 路由能力。
PR #35 是另一条 DAG 对照与失败节点恢复路线，本次没有合入该未合并分支。

## 插件接口与兼容记录

插件保留 `0.9.0` 版本，同时注册 `refractrouter_validate` 和 `refractrouter_task`。
前者支持执行模式与 K3 分阶段实验，后者继续使用主分支的文本任务运行器。
文本任务不接收基准专用的 `--selection-policy` 参数；路由和费用逻辑仍由 Python 负责。

K3 配置快照覆盖 `src/refractrouter/` 和 `experiments/` 的顶层 Python 文件。
因此，合入独立任务模块也会触发跨版本校验。此次只在兼容清单中新增一条精确记录，绑定
成功 K3 基线的原始索引、源配置及当前目标配置，不修改原始报告或放宽默认校验。

相对于已验证的基线恢复实现，共享客户端只新增并发日志锁、供文本任务使用的调用克隆，
以及默认空值的请求 ID 前缀。K3 路径未调用克隆方法；提示、模型、任务、计费解析、
契约与校准不变。新增模块属于独立的文本任务及 DAG 研究入口。
兼容清单列出相对于原始基线的所有新增或变化文件及其哈希。

## 已有实验的下一阶段

`reports/v0.5-k3-resume-admission-2/output/` 已完成 28 次调用并停在
`node-review-ready`，仍需完成 21 项独立节点评审、质量约束选模、DAG 组合及最终盲评。
本次集成没有执行这些步骤，也没有发起付费调用。

这份材料的 `compose` 和 `finalize` 仍严格绑定原代码快照，不能直接在本次合并后的
快照上执行。继续这轮实验时应从冻结提交
`89ad6323b89b654763b6ef3651d0c53af96a3370` 创建独立工作目录，使用新输出路径；
或另行完成显式的阶段迁移设计及验证。此次新增的基线兼容记录不授权阶段迁移，
也不意味着需要重新执行已经成功的 K3、参考路线或节点探针。

K3 HTML 的确定性章节评分仍存在已记录的编号标题口径差异；这不是独立质量评分。
最终质量与成本收益尚无结论，Issue #36 保持开放。

## 本地验证

- `uv run pytest`：333 项通过，包含 TypeScript 宿主契约与实际 Python 子进程预检。
- 插件 `typecheck` 和 `build`：通过。
- `canonical_runner.py` 的固定任务模拟验证：通过。
- `real_runner.py --phase final` 的 OpenAI 清单零调用预检：通过。
- 新增回归检查确认既有 `node-review-ready` 材料不能绕过快照校验进入组合阶段。
- 变更文件未检出扫描规则覆盖的密钥模式，`git diff --check` 通过。

上述检查均未调用付费模型；GitHub CI 状态以 PR 页面为准。
